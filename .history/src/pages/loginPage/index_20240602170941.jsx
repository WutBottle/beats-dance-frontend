import styles from './index.module.less'
import { Button } from 'antd'
export default () => {
  return (
    <div className={styles.loginWrapper}>
      <Button type="primary">loginPage</Button>
    </div>
  )
}
